<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>Nouveau ticket créé</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            line-height: 1.6;
            color: #333;
        }
        .container {
            max-width: 600px;
            margin: 0 auto;
            padding: 20px;
        }
        .header {
            background-color: #007bff;
            color: white;
            padding: 20px;
            text-align: center;
            border-radius: 5px 5px 0 0;
        }
        .content {
            background-color: #f8f9fa;
            padding: 20px;
            border-radius: 0 0 5px 5px;
        }
        .priority {
            display: inline-block;
            padding: 5px 10px;
            border-radius: 3px;
            color: white;
            font-size: 14px;
        }
        .priority-basse { background-color: #28a745; }
        .priority-normale { background-color: #007bff; }
        .priority-haute { background-color: #ffc107; }
        .priority-urgente { background-color: #dc3545; }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <h1>Nouveau ticket créé</h1>
        </div>
        <div class="content">
            <p>Bonjour {{ utilisateur.nom }},</p>
            
            <p>Votre ticket a été créé avec succès. Voici les détails :</p>
            
            <h2>{{ ticket.titre }}</h2>
            
            <p><strong>Description :</strong><br>
            {{ ticket.description }}</p>
            
            <p><strong>Priorité :</strong>
            <span class="priority priority-{{ ticket.priorite|lower }}">{{ ticket.priorite }}</span></p>
            
            <p><strong>Statut :</strong> {{ ticket.statut }}</p>
            
            <p><strong>Date de création :</strong> {{ ticket.dateCreation|date('d/m/Y H:i') }}</p>
            
            <p>Vous serez notifié de tout changement concernant ce ticket.</p>
            
            <p>Cordialement,<br>
            L'équipe ZenTicket</p>
        </div>
    </div>
</body>
</html>
