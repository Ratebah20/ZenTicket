{# templates/emails/statut_change.html.twig #}
<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>{% if new_status == 'résolu' %}Ticket résolu{% else %}Ticket clôturé{% endif %}</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            line-height: 1.6;
            color: #333;
            max-width: 600px;
            margin: 0 auto;
            padding: 20px;
        }
        .header {
            background-color: {% if new_status == 'résolu' %}#28a745{% else %}#6c757d{% endif %};
            color: white;
            padding: 20px;
            border-radius: 5px;
            margin-bottom: 20px;
        }
        .content {
            background-color: #f8f9fa;
            padding: 20px;
            border-radius: 5px;
        }
        .footer {
            margin-top: 20px;
            padding-top: 20px;
            border-top: 1px solid #dee2e6;
            font-size: 0.9em;
            color: #6c757d;
        }
    </style>
</head>
<body>
    <div class="header">
        <h1>{% if new_status == 'résolu' %}Votre ticket a été résolu{% else %}Votre ticket a été clôturé{% endif %}</h1>
    </div>

    <div class="content">
        <p>Bonjour,</p>

        <p>{% if new_status == 'résolu' %}
            Nous sommes heureux de vous informer que votre ticket a été résolu par notre équipe technique.
        {% else %}
            Votre ticket a été clôturé suite à la validation de la solution proposée.
        {% endif %}</p>

        <h2>Détails du ticket :</h2>
        <ul>
            <li><strong>Titre :</strong> {{ ticket.titre }}</li>
            <li><strong>Description :</strong> {{ ticket.description }}</li>
            {% if ticket.solution %}
                <li><strong>Solution :</strong> {{ ticket.solution }}</li>
            {% endif %}
        </ul>

        <p>Si vous avez des questions ou si vous souhaitez rouvrir ce ticket, n'hésitez pas à nous contacter.</p>
        
        <p>
            <a href="{{ url('app_ticket_show', {'id': ticket.id}) }}">Voir le ticket</a>
        </p>
    </div>

    <div class="footer">
        <p>Cordialement,<br>L'équipe ZenTicket</p>
        <p>Cet email a été envoyé automatiquement par le système de gestion des tickets.</p>
    </div>
</body>
</html>
